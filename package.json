{
    "name": "it-skillhub-root",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build && cd .. && mkdir -p dist && cp -r client/dist/* dist/"
    },
    "dependencies": {
        "bcryptjs": "^3.0.3",
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "jsonwebtoken": "^9.0.3",
        "mongoose": "^9.0.2",
        "multer": "^2.0.2",
        "pdf-lib": "^1.17.1",
        "razorpay": "^2.9.6"
    }
}